import { styled } from "@mui/material";

const ContainerLotties = styled("div")({
    display: "flex",
    alignItems: "center",
    justifyContent:"center",
    position: "absolute",
    zIndex: "10",
    width: "100%",
    height: "100%",
});


export { ContainerLotties };
