import { styled } from "@mui/material";


export const App_wrapper = styled("section")(({ theme }) => ({
    width: "100%",
    height: "100dvh",
    background: theme.palette.background.default,
}));

export const App_container = styled("main")({
    width: "100%",
    height:"100%"
})


